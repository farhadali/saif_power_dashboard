import 'package:flutter/material.dart';

class OrderFragementScreen extends StatelessWidget {
  const OrderFragementScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Order Fragement"),
      ),
    );
  }
}
