import 'package:flutter/material.dart';

class FavoritesFragementScree extends StatelessWidget {
  const FavoritesFragementScree({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Favorite Fragement"),
      ),
    );
  }
}
